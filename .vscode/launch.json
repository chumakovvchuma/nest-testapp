{
    "version": "0.2.0",
    "configurations": [
        
        
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "Launch Program",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${file}"
        },
        {
            "type": "node-terminal",
            "name": "Run Script: server:dev",
            "request": "launch",
            "command": "npm run server:dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}